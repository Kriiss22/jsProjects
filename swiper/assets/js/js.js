let isAnimating = false;
let cardPullDistance = 0; // distancia que la carta se está arrastrando


function startDrag (event){
    if(isAnimating) return

    const actualCard = event.target.closest('article') //cogemos el primer articulo ( carta)

    //


}